@model IEnumerable<Chipotle.ItemDetails.Item>

<div class="container">
    @foreach (var item in Model)
    {
        <div class="flot">
            <img src="@Url.Content(item.Url)" alt="Image" />
            <p>@item.Name</p>
        </div>
    }

</div>